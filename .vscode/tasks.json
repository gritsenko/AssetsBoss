{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-server",
      "command": "dotnet",
      "type": "process",
      "args": ["build", "${workspaceFolder}/src/AssetsBoss.Server/AssetsBoss.Server.csproj"],
      "problemMatcher": "$msCompile"
    }
  ]
}
