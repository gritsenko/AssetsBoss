# Приватные провайдер-расширения подключаются локально и не попадают в публичный репозиторий.
# Игнорируем всё содержимое каталога, кроме самого этого файла (чтобы каталог-шов сохранялся).
*
!.gitignore
